# Yaesu FTM-200D data port (phase 9)

What the FTM-200D sends out of its data port, and what the FTM-200 backend
(WP9.3–9.5) has to handle. **Status: WP9.1 in progress.** The format below comes from
the user's `ftm200_aprs_bridge.py` and hasn't been checked against a capture yet.
Anything marked *unconfirmed* stays that way until a capture shows it.

## Hardware

```
FTM-200D DATA jack ── Yaesu SCU-66 (USB serial) ── Pi USB ── /dev/serial/by-id/usb-…
```

- The cable shows up as a USB serial port. The Digirig's CP2102 is the only other
  USB serial port, so "the one `/dev/serial/by-id` entry that isn't CP210x" finds it.
  Record the actual by-id name, VID:PID and chip here once it's plugged in: *unknown*.
- The user's older scripts call the cable the **SCU-56**, while the project docs say
  **SCU-66**. Check which one is actually in use.
- gpsd leaves the port alone (`USBAUTO="false"` in `deploy/gpsd.default`). The Pi has
  no ModemManager or brltty that might probe it, and `pi` is in `dialout`.
- The bridge holds RTS and DTR low. The capture tool does the same, until we know
  whether the radio or cable pays attention to them.

## Radio settings

Menu names are as best known and need checking against the FTM-200D APRS manual.
Update this table with the exact names and values that were used.

| Setting | Value | Why |
|---|---|---|
| APRS modem | On, on the band tuned to 144.390 | The radio only decodes APRS on the APRS band |
| COM port output | PACKET | Other choices are GPS (NMEA) and WAYPOINT |
| COM port speed | 9600 (bridge default) | Must match `--baud`. `--scan` finds it |
| APRS filters (Mic-E, position, weather, object, item, status, other) | All on | Assumed to limit what gets output as well as what's shown. *Unconfirmed* |
| APRS data speed | 1200 | Standard APRS |

## Line format (from the bridge, *unconfirmed*)

Each packet arrives as two lines:

```
KF0KBP-1>APLRG1,WIDE1-1 [09/24/26 12:00:00] <UI R>:
!3947.00N/09533.00W#comment
```

1. **Header:** `SRC>DEST,PATH`, then a space, a bracketed timestamp, an optional
   `<…>` frame-type tag, and a trailing `:`. The bridge matches it with
   `^([^ >\r\n]+>[^\s\[]+)\s+\[[^\]]+\](?:\s+<[^>]*>)?\s*:\s*$`.
2. **Info field** on the next line, unchanged.

The bridge builds TNC2 as `header + ":" + info` and ignores every line that
doesn't follow a header.

## Questions the capture has to answer

Fill each one in with evidence (the capture file name and the line) before
ticking WP9.1.

- [ ] Serial settings: baud, 8N1, line ending (`\r\n`?), idle output between packets
- [ ] Header: timestamp format and clock source (radio clock or GPS), and every `<…>`
      tag seen (`UI R`, `UI C`, …?) with what it means
- [ ] Path: does it keep the H-bit `*` on digipeated calls? This is needed for
      `heard_direct` and `rf_direct`, and also for the iGate
- [ ] Info field: is it complete and unchanged? Check against the same packet decoded
      by Direwolf (see *Capture procedure*). Mic-E, messages with `{`, non-ASCII bytes,
      trailing spaces, embedded CR/LF, and very long packets
- [ ] Which packet types come out: positions, Mic-E, messages (to us and to others),
      acks, objects, items, status, telemetry, weather, third-party `}`, non-APRS
      AX.25 frames. Do the APRS filters change this?
- [ ] Duplicates: does it output every digipeated copy, or drop the ones it has
      already heard?
- [ ] Are packets the FTM-200 **transmits** (its own beacons and messages) echoed on
      the port? How are they marked?
- [ ] Does anything else appear (NMEA, status lines, a banner at power-on)?
- [ ] Does the radio react to anything sent *to* the port? That's WP9.5, so only
      after the passive capture, and only with approval, since it could transmit

## Capture procedure

The capture tool is `tools/ftm200_capture.py`. It's read-only and runs on the
Pi's system python3.

1. Plug the SCU-66 into the Pi and set up the radio as above.
   `ls -l /dev/serial/by-id/` should now show a second entry.
2. Find the speed if it's not known: `python3 ~/aprsx/tools/ftm200_capture.py --scan`
   (60 s at each speed; the right one gives printable text and packet headers).
3. Record: `python3 ~/aprsx/tools/ftm200_capture.py --baud 9600 --seconds 3600`.
   This writes `~/ftm200-capture-<time>.raw` (exact bytes) and `.log` (timestamped
   lines).
4. **Reference copy:** leave `aprsx-core` running on the Digirig and UV-5R during the
   capture. The same over-the-air packets are then logged by Direwolf in the core's
   `packets` table, so each FTM-200 line can be compared with Direwolf's TNC2
   for the same packet (match on source and info within a few seconds).
5. For packet types that don't come up by themselves (messages, objects, Mic-E),
   send known packets from KF0KBP-7 on the Digirig, **only with the user's OK**, since
   that transmits. Also note which of the FTM-200's own beacons and messages were
   sent during the capture.
6. Copy the files into `tests/data/ftm200/` for WP9.6. Before committing, cut them down
   to a representative set.

## Findings

*None yet: no capture has been made.*
